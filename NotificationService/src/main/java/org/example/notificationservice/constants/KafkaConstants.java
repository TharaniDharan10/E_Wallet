package org.example.notificationservice.constants;

public interface KafkaConstants {   //either we can make this as class and make public static final in all keys (or) make it an interface so that we need not make public static final

    String USER_CREATION_TOPIC = "user_created";
}
